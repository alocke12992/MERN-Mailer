import React from 'react';

const Landing = () => (
  <div style={{ textAlign: 'center' }}>
    <h2>Welcome to Mern-Mail</h2>
    A "Mern" way to connect with your users
  </div>
);

export default Landing;
